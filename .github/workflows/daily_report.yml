name: Global Market Monitor Parallel

on:
  schedule:
    - cron: '30 10 * * 1-5' # 台北時間 18:30 (UTC 10:30)
  workflow_dispatch:
    inputs:
      market_choice:
        description: '選擇執行市場'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - tw
          - us
          - hk
          - cn
          - jp
          - kr

jobs:
  global-market-analysis:
    name: ${{ matrix.market.emoji }} ${{ matrix.market.name }} Download
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        market:
          - { id: "tw", name: "Taiwan", emoji: "🇹🇼" }
          - { id: "us", name: "US",     emoji: "🇺🇸" }
          - { id: "hk", name: "HK",     emoji: "🇭🇰" }
          - { id: "cn", name: "China",  emoji: "🇨🇳" }
          - { id: "jp", name: "Japan",  emoji: "🇯🇵" }
          - { id: "kr", name: "Korea",  emoji: "🇰🇷" }

    steps:
      - uses: actions/checkout@v4
        if: |
          github.event_name == 'schedule' || 
          github.event.inputs.market_choice == 'all' || 
          github.event.inputs.market_choice == matrix.market.id

      - name: Set up Python
        if: success()
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        if: success()
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Market Download
        if: success()
        env:
          # 💡 這裡要把你在 GitHub Repo -> Settings -> Secrets 設好的變數接進去
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GDRIVE_SERVICE_ACCOUNT: ${{ secrets.GDRIVE_SERVICE_ACCOUNT }}
        # 💡 注意這裡的傳參方式要跟你的 main.py 一致
        run: python main.py ${{ matrix.market.id }}
